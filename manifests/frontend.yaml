apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: blu-live-clinic
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: irandejonah/blu-live-clinic-frontend:v6
        ports:
        - containerPort: 80